_target_: pytorch_ie.taskmodules.TransformerTokenClassificationTaskModule

#tokenizer_name_or_path: ${transformer_model} # transformer_model is specified in config_rel.yaml
tokenizer_name_or_path: bert-base-uncased

#partition_annotation: ${partition_annotation_name}
entity_annotation: entities
#max_window: ${max_input_size}
#window_overlap: 64

#show_statistics: true

#include_ill_formed_predictions: false
