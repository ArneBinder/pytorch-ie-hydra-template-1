select_n:
  _processor_: pie_datasets.DatasetDict.select
  split: train
  # take all data per default
  stop: null

select_n_test:
  _processor_: pie_datasets.DatasetDict.select
  split: test
  # take all data per default
  stop: null

select_n_validation:
  _processor_: pie_datasets.DatasetDict.select
  split: validation
  # take all data per default
  stop: null
