_target_: src.metrics.F1Metric
layer: ???
show_as_markdown: true
exclude_annotation_fields: ["score"]
# label_field: label  # field of the annotation that contains the label. if set, calculate per label metrics
# labels: ???  # all possible labels. required if label_field is set
